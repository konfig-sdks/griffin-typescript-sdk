import { Griffin } from "./index";

describe("griffin-typescript-sdk", () => {
    it("initialize client", async () => {
        const griffin = new Griffin({
            apiKey: "API_KEY",
        });
    });
});
